<form method="post" action="{{ path('app.remote_host.delete', {'id': remote_host.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ remote_host.id) }}">
    <button class="btn">Delete</button>
</form>
