{% extends 'base.html.twig' %}

{% block title %}SAL | Monitor{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Users/TwardyDyskus/Desktop/SAL/src/Controller/MainController.php'|file_link(0) }}">src/Controller/MainController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/TwardyDyskus/Desktop/SAL/templates/main/index.html.twig'|file_link(0) }}">templates/main/index.html.twig</a></code></li>
    </ul>
</div>
<div>
    <table class="table table-dark table-striped">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Remote Host</th>
            <th scope="col">Status</th>
            <th scope="col">Notification</th>
            <th scope="col">Time Stamp</th>
            <th scope="col">Mute Time</th>
            <th scope="col">Log Time Stamp</th>
            <th scope="col">Log Message</th>
        </tr>
        </thead>
        <tbody>
        {% for log in logs %}
            <tr>
                <th scope="row">{{ log.id }}</th>
                <td>{{ log[name] }}</td>
                <td>{{ log.status }}</td>
                <td>{{ log.notification }}</td>
                <td>{{ log.timeStamp.date }}</td>
                <td>{{ log.muteTime.date }}</td>
                <td>{{ log.logData.timeStamp.date }}</td>
                <td>{{ log.logData.message }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
